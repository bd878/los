#include <stdio.h>

/*
 * Simple program that
 * accepts program name,
 * traverses /proc for given
 * substring
 * and outputs processes
 * that matched this substring
 * its cgroups
 */

int main()
{
	printf("test\n");
	return 0;
}
